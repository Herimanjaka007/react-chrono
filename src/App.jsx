import React from 'react';

const App = () => {
  return (
    "test"
  );
}

const Button = ({buttonValue}) => {
  return <button className="btn btn-primary">{buttonValue}</button>
}

export default App;
